def count_regs(coi: str, countries: list[str]) -> int:
    idx: int = 0
    total: int = 0
    while idx < len(countries):
        if countries[idx] == coi:
            total += 1
        idx += 1
    return total


def hehe(n: int) -> int:
    return 0


def hehe2(n: int) -> int:
    return 0
